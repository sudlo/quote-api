name: Build and Push Docker Image (Hardcoded)

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      # Insecure hardcoded credentials as requested.
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: sudlo
          password: dckr_pat_7k--mM56s_GNIsg0aFTLBqxApoY

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: sudlo/quote-api:latest
